import { CreatePasswordMatchingDirective } from './create-password-matching.directive';

describe('CreatePasswordMatchingDirective', () => {
  it('should create an instance', () => {
    const directive = new CreatePasswordMatchingDirective();
    expect(directive).toBeTruthy();
  });
});
